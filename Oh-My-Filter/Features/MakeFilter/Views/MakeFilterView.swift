import SwiftUI

struct MakeFilterView: View {
  var body: some View {
    TabScreenView(
      title: "만들기",
      subtitle: "나만의 필터를 제작하는 진입점을 배치합니다.",
      symbolName: IconToken.magic.symbolName
    )
  }
}
